name: Flutter CI/CD Full Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Descargar código
      - name: Checkout
        uses: actions/checkout@v3

      # 2. Instalar Flutter
      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.1"

      # 3. Cache para acelerar builds
      - name: Cache Flutter packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.pub-cache
            build/
          key: ${{ runner.os }}-flutter-${{ hashFiles('**/pubspec.lock') }}

      # 4. Instalar dependencias
      - name: Flutter pub get
        run: flutter pub get

      # 5. ANALIZAR Código (Linter)
      - name: Flutter analyze (allow warnings)
        run: flutter analyze --no-fatal-warnings --no-fatal-infos

      # 6. EJECUTAR TESTS
      - name: Run tests
        run: flutter test --coverage --no-pub || true

      # 7. GUARDAR REPORTE DE COBERTURA
      - name: Save coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/lcov.info

      # 🚀 8. BUILD WEB
      - name: Build Web
        run: flutter build web --release

      # # 🚀 9. Deploy a Firebase Hosting
      # - name: Deploy to Firebase Hosting
      #   uses: w9jds/firebase-action@v2.2.0
      #   with:
      #     args: deploy --only hosting
      #   env:
      #     FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        
      # 8. COMPILAR APK DEBUG
      # - name: Build APK (debug)
      #   run: flutter build apk --debug

      # - name: Upload APK Debug
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: app-debug.apk
      #     path: build/app/outputs/flutter-apk/app-debug.apk

      # 9. COMPILAR APK RELEASE
      # - name: Build APK (release)
      #   run: flutter build apk --release

      # - name: Upload APK Release
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: app-release.apk
      #     path: build/app/outputs/flutter-apk/app-release.apk

      # 10. COMPILAR AAB PARA PLAY STORE
      # - name: Build AAB
      #   run: flutter build appbundle --release

      # - name: Upload AAB
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: app-release.aab
      #     path: build/app/outputs/bundle/release/app-release.aab
